import { DirectivaRatonEncima } from './directiva-raton-encima';

describe('DirectivaRatonEncima', () => {
  it('should create an instance', () => {
    const directive = new DirectivaRatonEncima();
    expect(directive).toBeTruthy();
  });
});
