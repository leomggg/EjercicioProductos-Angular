import { AvatarDefaultPipe } from './avatar-default-pipe';

describe('AvatarDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
